function addBlock3() {
    //////// FLOOR ////////

    // Weggie Maths Room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Music room theory done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Music room prac done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // tate office done

    // Boehm Maths room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Year 10 meeting room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Kee office done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Boehm office done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Colab done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Class opposite colab done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Class at end of hallway done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Small room at end of hallway
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // toilets done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Instumental rooms done
    rid(createMarker(826, 203, 80, 75, 4.5)).onclick = () => {
        partInfo.textContent = "These are the instrumental rooms, you should only have to go here if you have an instrument lesson or you have permission.";
    };

    //duffett done

    //3f24 done

    //////// GROUND ////////

    // Instumental rooms done
    rid(createMarker(826, 203, 80, 75, 4.5)).onclick = () => {
        partInfo.textContent = "These lead to the instrumental rooms, there is little chance you would need to go here.";
    };

    // Chemistry lab done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is the Chemistry Lab.";
    };

    // Bio lab
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is the Biology Lab.";
    };

    // Physics lab
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is the Physics Lab.";
    };

    // Kikuchi office done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is Mr Kikuchi's office";
    };

    // emergencxy shower done

    // small Wood tech room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is the wood tech room.";
    };

    // Green's officedone
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is the staff office.";
    };

    // HPAP room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is Human Perfomance Lab.";
    };

    // Glass room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This is the Human Performance Centre.";
    };

    // 10 pe room
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This classroom is used for...";
    };

    // Homegroup done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "This classroom is used for...";
    };

    // workout room done
}