function addBlock1() {
    //////// FLOOR ////////

    // History room done
    addRoom().onclick = () => {
        partInfo.textContent = "";
    };

    // Common room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // English room
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Masked theatre done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Rooms next to masked theatre? done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Dance room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Toilets done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Business room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Old English room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // IT room done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // LATS done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Mr Fourie office done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Mrs Prior office, other teachers? Forrest
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Geography room done

    // 1f20 done

    //////// GROUND ////////

    // Student Services done
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };

    // Library
    createMarker(0, 0, 0, 0, 4.5).onclick = () => {
        partInfo.textContent = "";
    };
}
